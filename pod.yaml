apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app.kubernetes.io/name: nginx
spec:
  containers:
  - name: nginx-container
    image: nginx
    command: ["bin/echo"]
    args: ["Hello","world!"]
   
  
